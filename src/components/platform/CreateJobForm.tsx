'use client';
import { useState } from 'react'; import Link from 'next/link'; import { CheckCircle2 } from 'lucide-react'; import { useAuthContext } from '@/components/providers/AuthProvider'; import { createJob } from '@/lib/db'; import { Button } from '@/components/ui/Button'; import { Field,TextareaField } from '@/components/ui/Field';
export function CreateJobForm(){const {org}=useAuthContext();const [busy,setBusy]=useState(false);const [success,setSuccess]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);await createJob({employerId:org.employer?.id??'',title:String(f.get('title')),description:String(f.get('description')),requiredSkills:String(f.get('skills')).split(',').map(x=>x.trim()).filter(Boolean),location:String(f.get('location')),dailyWage:Number(f.get('wage')),status:'open'});setBusy(false);setSuccess(true)}if(success)return <div className="mx-auto max-w-xl rounded-[12px] bg-white p-8 text-center shadow-[0_2px_6px_oklch(23%_0.074_255_/_0.07)]"><CheckCircle2 className="mx-auto text-opportunity-700" size={44}/><h1 className="mt-4 text-2xl font-bold text-navy-900">เผยแพร่ประกาศงานแล้ว</h1><p className="mt-2 text-slate-700">ระบบจะเริ่มค้นหาผู้สมัครที่มีทักษะและความพร้อมตรงกับงานนี้</p><Link href="/employer/matches" className="mt-6 inline-flex min-h-11 items-center rounded-[10px] bg-navy-900 px-5 font-bold text-white">ดูผู้สมัครที่เหมาะสม</Link></div>;return <div className="page-enter mx-auto max-w-3xl"><h1 className="text-3xl font-extrabold text-navy-900">ประกาศงานใหม่</h1><p className="mt-2 text-slate-700">ข้อมูลที่ชัดเจนช่วยให้การจับคู่แม่นยำและผู้สมัครตัดสินใจได้อย่างมั่นใจ</p><form onSubmit={submit} className="mt-7 grid gap-5 rounded-[12px] bg-white p-5 shadow-[0_2px_6px_oklch(23%_0.074_255_/_0.07)] sm:p-7"><Field name="title" label="ชื่อตำแหน่งงาน" required/><TextareaField name="description" label="รายละเอียดงาน" required/><Field name="skills" label="ทักษะที่ต้องการ" required hint="คั่นแต่ละทักษะด้วยเครื่องหมายจุลภาค"/><div className="grid gap-5 sm:grid-cols-2"><Field name="location" label="สถานที่ทำงาน" required/><Field name="wage" label="ค่าจ้างต่อวัน (บาท)" type="number" min={1} required/></div><div className="flex justify-end"><Button type="submit" size="lg" disabled={busy}>{busy?'กำลังเผยแพร่…':'เผยแพร่ประกาศงาน'}</Button></div></form></div>}
